<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
<tr>
<% end %>
    <td class="xxx"><%= line_item.quantity  %>&nbsp;&times;</td>
    <td class="xxx"><p><%= line_item.product.title %></p></td>
    <td class="item_price">
        <%= number_to_currency(line_item.total_price) %>
    </td>
</tr>